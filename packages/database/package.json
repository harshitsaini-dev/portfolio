{
  "name": "@portfolio/database",
  "version": "0.0.0",
  "private": true,
  "description": "Database package. Owns the D1 migration smoke test; the repository/query layer is a future phase.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node scripts/migrations-smoke-test.mjs"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
