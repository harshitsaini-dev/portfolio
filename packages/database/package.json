{
  "name": "@portfolio/database",
  "version": "0.0.0",
  "private": true,
  "description": "Database package. Owns the D1 migration smoke test; the repository/query layer is a future phase.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node scripts/uuid-tests.mjs && node scripts/migrations-smoke-test.mjs && node scripts/repository-tests.mjs && node scripts/d1-binding-tests.mjs && node scripts/d1-type-compatibility.mjs"
  },
  "dependencies": {
    "@portfolio/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
