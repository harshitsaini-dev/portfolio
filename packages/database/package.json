{
  "name": "@portfolio/database",
  "version": "0.0.0",
  "private": true,
  "description": "Database access package (skeleton, no schema/queries yet — D1 integration is a future phase).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
