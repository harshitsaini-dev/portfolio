{
  "name": "@portfolio/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI package. Currently provides the semantic design tokens consumed by both apps; no React components yet.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
