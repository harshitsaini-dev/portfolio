{
  "name": "@portfolio/schemas",
  "version": "0.0.0",
  "private": true,
  "description": "Shared validation schemas package (skeleton, no domain schemas yet).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
